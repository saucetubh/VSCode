module state_register3 (
    input  wire       clk,
    input  wire       rst_n,
    input  wire       load,
    input  wire [2:0] hold_mask,
    input  wire       scrub,
    input  wire [2:0] next_state,
    output wire [2:0] curr_state
);

endmodule